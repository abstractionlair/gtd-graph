# Provenance

## Public release

I pushed this to GitHub on 2026-07-07 as "Initial public release." Only 3
commits total — I squashed the history at release.

## Real timeframe

I wasn't careful with git here, and there isn't much else to go on
either: everything under `People/`, `Projects/`, `Actions/`, `Notes/`,
and `Decisions/` is explicitly synthetic example data (see the README),
not a record of real use — so the 2026-01-15 dates in there don't tell
you anything about when this was actually built. What's here is the
skeleton: the base of a system I actually used for real. The real
version used git for its own persistence and had actual personal data in
it, which obviously isn't something that belongs in a public repo, so
what you're looking at is the stripped-down, example-data version of it.
The project predates its public release; I'm not claiming a more
specific date than that.

## Account-wide context

This repo was part of a July 2026 mass-update pass I did across the
account. See my profile README
(https://github.com/abstractionlair/abstractionlair) for the
account-wide explanation.

## AI assistance

See this repo's own README `## Provenance` section: the ontology (node
and edge types, fields, linking rules) is mine; the schemas, templates,
and examples are agent-written, with review deliberately delegated too.
